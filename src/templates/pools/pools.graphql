type Pool @entity {
  id: ID!
  chainId: Int! @index
  address: String! @index
  exchange: String! @index
  name: String
  symbol: String
  tokens: [String!]!
  symbols: [String!]!
  decimals: [Int!]!
  type: String!
  createdAtBlock: Int!
  createdAt: DateTime!
}
