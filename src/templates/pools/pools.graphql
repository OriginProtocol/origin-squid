type Pool @entity {
  id: ID!
  chainId: Int! @index
  address: String! @index
  exchange: String! @index
  name: String
  symbol: String
  tokens: [String!]!
  type: String!
  createdAtBlock: Int!
  createdAt: DateTime!
}
