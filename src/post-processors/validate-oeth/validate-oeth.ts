import { OETHCurveLP } from '../../model'
import { Context } from '../../processor'

export const process = (ctx: Context) => {
  // for (const block of ctx.blocks) {
  //   if (block.header.height) {
  //     ctx.store.findOne(OETHCurveLP, {})
  //   }
  // }
}

const expectations = {}
